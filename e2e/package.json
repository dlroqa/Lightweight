{
  "name": "lightagent-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Headless render + smoke of the panel against a live gateway and agent server.",
  "type": "module",
  "scripts": {
    "render": "node render.mjs"
  },
  "devDependencies": {
    "playwright": "1.55.0"
  }
}
