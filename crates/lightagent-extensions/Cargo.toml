[package]
name = "lightagent-extensions"
description = "Installable capability bundles for the Lightagent runtime: an extension packages skills, MCP-server declarations and persona instructions into one directory, discovered and composed into a run."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
lightagent-core.workspace = true
serde = { workspace = true }
serde_json.workspace = true
